# Прогнозирование стоимости автомобилей с пробегом

> **«Не бит, не крашен» — точная оценка за секунду.**  
> Модель машинного обучения для сервиса по продаже подержанных автомобилей

---

## 📊 Описание проекта

Проект разработан для сервиса **«Не бит, не крашен»** — платформы, где пользователи хотят быстро узнать рыночную стоимость своего автомобиля по техническим характеристикам.  

Цель: построить **точную, быструю и надёжную** модель, предсказывающую цену автомобиля (в евро) на основе 12 признаков: год регистрации, мощность, пробег, тип коробки передач, марка, модель, тип топлива и другие.

**Критерии заказчика:**
- ✅ **Качество**: RMSE < 2500 €  
- ✅ **Скорость обучения**: < 10 секунд  
- ✅ **Скорость предсказания**: < 1 секунда  

---

## 📊 Данные

- **Объём**: 354 369 записей  
- **Признаки**:  
  - Числовые: `price`, `registration_year`, `power`, `kilometer`  
  - Категориальные: `gearbox`, `model`, `fuel_type`, `repaired`, `brand`, `vehicle_type`  
- **Пропуски**: до 71 154 в `repaired`, до 37 490 в `vehicle_type`  
- **Аномалии**: цены < 100 €, мощность = 0 л.с., пробег = 150 000 км (бонусный бин)  

> 💡 **Ключевое открытие**: пробег `kilometer` — не непрерывная величина, а **закодированный категориальный признак** (максимум = 150 000 км). Это типичный артефакт сбора данных на платформах.

---

## 🔧 Предобработка данных

| Этап | Действие |
|------|----------|
| **Названия** | Приведены к `snake_case` |
| **Пропуски** | Заполнены `'unknown'` (категориальные) и `'no'` (`repaired`) |
| **Дубликаты** | Удалены **только в обучающей выборке** — тестовая осталась нетронутой |
| **Аномалии** | Удалены: `price < 100`, `power < 50`, `power > 800`, `registration_year > 2016` |
| **Неинформативные признаки** | Удалены: `date_crawled`, `date_created`, `last_seen`, `postal_code`, `registration_month`, `number_of_pictures` |
| **Мультиколлинеарность** | Удалены `brand` и `vehicle_type` — их информация полностью дублируется `model` |
| **Кодирование** | `OneHotEncoder` для `LinearRegression`, **встроенные кодировщики** для `CatBoost` и `LightGBM` |

---

## 🔍 Исследовательский анализ

- **Цена**: сильная **правосторонняя асимметрия** — большинство авто в диапазоне 100–7000 €, но есть «хвост» до 20 000 €.  
- **Пробег**: 75% авто имеют **150 000 км** — это не реальный пробег, а **дефолтное значение** на платформе.  
- **Модель**: **самый сильный предиктор** цены (Phi-K = 0.57) — даже сильнее, чем год или мощность.  
- **Бренд и тип кузова**: сильно скоррелированы с моделью → удалены для упрощения модели.  
- **Тип топлива**: слабая корреляция (Phi-K = 0.11) → не влияет на цену существенно.

> ✅ **Среднестатистический автомобиль**:  
> **Volkswagen Golf**, 2002 г., 122 л.с., 128 493 км, ручная КПП, бензин, без ремонта → **~4928 €**

---

## 📈 Корреляционный анализ

| Признак | Корреляция с `price` (Phi-K) | Влияние |
|--------|------------------------------|---------|
| `model` | **0.57** | ⭐⭐⭐⭐⭐ |
| `registration_year` | 0.47 | ⭐⭐⭐⭐ |
| `power` | 0.46 | ⭐⭐⭐⭐ |
| `kilometer` | 0.33 | ⭐⭐⭐ |
| `repaired` | 0.30 | ⭐⭐⭐ |
| `gearbox` | 0.29 | ⭐⭐⭐ |
| `fuel_type` | 0.11 | ⭐ |

> 💡 **Phi-K** показал, что **категориальные признаки** (особенно `model`) — главные драйверы цены, что не видно при использовании только Пирсона/Спирмана.

---

## 🤖 Обучение моделей

| Модель | CV RMSE | Fit Time (с) | Predict Time (с) | Примечание |
|--------|---------|--------------|------------------|------------|
| **LinearRegression** | 2707 | 9.53 | 0.31 | Не подходит — RMSE > 2500 |
| **CatBoostRegressor** | 1761 | 21.55 | **0.30** | Точная, но медленная в обучении |
| **LGBMRegressor (tuned)** | **1732** | **3.19** | 1.11 | ✅ **Лучший баланс** |

### ✅ Подбор гиперпараметров (LGBM)

```python
param_dist = {
    'regressor__n_estimators': [100, 150],
    'regressor__learning_rate': [0.2, 0.3],
    'regressor__max_depth': [5, 7],
    'regressor__num_leaves': [31, 41],
}
